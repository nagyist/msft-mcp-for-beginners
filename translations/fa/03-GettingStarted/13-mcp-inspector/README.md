# اشکال‌زدایی با MCP Inspector

**MCP Inspector** یک ابزار حیاتی برای اشکال‌زدایی است که به شما امکان می‌دهد سرورهای MCP خود را به صورت تعاملی آزمایش و رفع اشکال کنید بدون اینکه به یک برنامه میزبان کامل هوش مصنوعی نیاز داشته باشید. می‌توانید آن را «پست‌من برای MCP» در نظر بگیرید - این ابزار یک رابط بصری ارائه می‌دهد تا درخواست‌ها را ارسال کنید، پاسخ‌ها را مشاهده کنید و بفهمید سرور شما چگونه رفتار می‌کند.

## چرا از MCP Inspector استفاده کنیم؟

هنگام ساخت سرورهای MCP، اغلب با این چالش‌ها مواجه می‌شوید:

- **"آیا سرور من حتی در حال اجرا است؟"** - Inspector وضعیت اتصال را نشان می‌دهد
- **"آیا ابزارهای من به‌درستی ثبت شده‌اند؟"** - Inspector همه ابزارهای موجود را فهرست می‌کند
- **"فرمت پاسخ چیست؟"** - Inspector پاسخ‌های کامل JSON را نمایش می‌دهد
- **"چرا این ابزار کار نمی‌کند؟"** - Inspector پیام‌های خطای دقیق را نشان می‌دهد

## پیش‌نیازها

- نصب Node.js نسخه 18 یا بالاتر
- npm (همراه با Node.js)
- یک سرور MCP برای آزمایش (ببینید [ماژول 3.1 - اولین سرور](../01-first-server/README.md))

## نصب

### گزینه 1: اجرا با npx (توصیه‌شده برای آزمایش سریع)

```bash
npx @modelcontextprotocol/inspector
```

### گزینه ۲: نصب به صورت سراسری

```bash
npm install -g @modelcontextprotocol/inspector
mcp-inspector
```

### گزینه ۳: افزودن به پروژه شما

```bash
cd your-mcp-server-project
npm install --save-dev @modelcontextprotocol/inspector
```

اضافه کنید به `package.json`:
```json
{
  "scripts": {
    "inspector": "mcp-inspector"
  }
}
```

---

## اتصال به سرور شما

### سرورهای stdio (فرآیند محلی)

برای سرورهایی که از طریق ورودی/خروجی استاندارد ارتباط برقرار می‌کنند:

```bash
# سرور پایتون
npx @modelcontextprotocol/inspector python -m your_server_module

# سرور Node.js
npx @modelcontextprotocol/inspector node ./build/index.js

# با متغیرهای محیطی
OPENAI_API_KEY=xxx npx @modelcontextprotocol/inspector python server.py
```

### سرورهای SSE/HTTP (شبکه)

برای سرورهایی که به عنوان سرویس HTTP اجرا می‌شوند:

1. ابتدا سرور خود را راه‌اندازی کنید:
   ```bash
   python server.py  # سرور در حال اجرا بر روی http://localhost:8080
   ```

2. Inspector را اجرا و متصل شوید:
   ```bash
   npx @modelcontextprotocol/inspector --sse http://localhost:8080/sse
   ```

---

## مرور رابط Inspector

وقتی Inspector اجرا می‌شود، یک رابط وب (معمولاً در `http://localhost:5173`) مشاهده خواهید کرد:

```
┌─────────────────────────────────────────────────────────────┐
│  MCP Inspector                              [Connected ✅]   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │   🔧 Tools  │  │ 📄 Resources│  │ 💬 Prompts  │         │
│  │    (3)      │  │    (2)      │  │    (1)      │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐ │
│  │  📋 Message Log                                       │ │
│  │  ─────────────────────────────────────────────────── │ │
│  │  → initialize                                         │ │
│  │  ← initialized (server info)                          │ │
│  │  → tools/list                                         │ │
│  │  ← tools (3 tools)                                    │ │
│  └───────────────────────────────────────────────────────┘ │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## آزمایش ابزارها

### فهرست ابزارهای موجود

1. روی تب **Tools** کلیک کنید
2. Inspector به‌طور خودکار `tools/list` را فراخوانی می‌کند
3. همه ابزارهای ثبت‌شده را خواهید دید همراه با:
   - نام ابزار
   - توضیحات
   - قالب ورودی (پارامترها)

### فراخوانی یک ابزار

1. یک ابزار از فهرست انتخاب کنید
2. پارامترهای لازم را در فرم پر کنید
3. روی **Run Tool** کلیک کنید
4. پاسخ را در بخش نتایج مشاهده کنید

**مثال: آزمایش یک ابزار ماشین حساب**

```
Tool: add
Parameters:
  a: 25
  b: 17

Response:
{
  "content": [
    {
      "type": "text",
      "text": "42"
    }
  ]
}
```

### اشکال‌زدایی خطاهای ابزار

وقتی یک ابزار شکست می‌خورد، Inspector نمایش می‌دهد:

```
Error Response:
{
  "error": {
    "code": -32602,
    "message": "Invalid params: 'b' is required"
  }
}
```

کدهای خطای رایج:
| کد | معنی |
|------|---------|
| -32700 | خطای تجزیه (JSON نامعتبر) |
| -32600 | درخواست نامعتبر |
| -32601 | روش یافت نشد |
| -32602 | پارامترهای نامعتبر |
| -32603 | خطای داخلی |

---

## آزمایش منابع

### فهرست منابع

1. روی تب **Resources** کلیک کنید
2. Inspector `resources/list` را فراخوانی می‌کند
3. مشاهده خواهید کرد:
   - URIهای منابع
   - نام‌ها و توضیحات
   - نوع MIME

### خواندن یک منبع

1. یک منبع انتخاب کنید
2. روی **Read Resource** کلیک کنید
3. محتوای بازگردانده شده را مشاهده کنید

**خروجی نمونه:**

```
Resource: file:///config/settings.json
Content-Type: application/json

{
  "config": {
    "debug": true,
    "maxConnections": 10
  }
}
```

---

## آزمایش دستورات

### فهرست دستورات

1. روی تب **Prompts** کلیک کنید
2. Inspector `prompts/list` را فراخوانی می‌کند
3. قالب‌های پرامپت موجود را مشاهده کنید

### دریافت یک پرامپت

1. یک پرامپت انتخاب کنید
2. هر آرگومان لازم را پر کنید
3. روی **Get Prompt** کلیک کنید
4. پیام‌های رندر شده پرامپت را ببینید

---

## تحلیل گزارش پیام‌ها

گزارش پیام‌ها همه پیام‌های پروتکل MCP را نشان می‌دهد:

```
14:32:01 → {"jsonrpc":"2.0","id":1,"method":"initialize",...}
14:32:01 ← {"jsonrpc":"2.0","id":1,"result":{"protocolVersion":"2025-11-25",...}}
14:32:02 → {"jsonrpc":"2.0","id":2,"method":"tools/list"}
14:32:02 ← {"jsonrpc":"2.0","id":2,"result":{"tools":[...]}}
14:32:05 → {"jsonrpc":"2.0","id":3,"method":"tools/call","params":{"name":"add",...}}
14:32:05 ← {"jsonrpc":"2.0","id":3,"result":{"content":[...]}}
```

### چه چیزهایی را بررسی کنیم

- **جفت‌های درخواست/پاسخ**: هر `→` باید یک `←` متناظر داشته باشد
- **پیام‌های خطا**: به دنبال `"error"` در پاسخ‌ها باشید
- **زمان‌بندی**: فاصله‌های زیاد ممکن است نشان‌دهنده مشکلات عملکردی باشند
- **نسخه پروتکل**: اطمینان حاصل کنید که سرور و کلاینت روی نسخه توافق دارند

---

## یکپارچه‌سازی با VS Code

می‌توانید Inspector را مستقیماً از VS Code اجرا کنید:

### استفاده از launch.json

به `.vscode/launch.json` اضافه کنید:

```json
{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug with MCP Inspector",
      "type": "node",
      "request": "launch",
      "runtimeExecutable": "npx",
      "runtimeArgs": [
        "@modelcontextprotocol/inspector",
        "python",
        "${workspaceFolder}/server.py"
      ],
      "console": "integratedTerminal"
    },
    {
      "name": "Debug SSE Server with Inspector",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:5173",
      "preLaunchTask": "Start MCP Inspector"
    }
  ]
}
```

### استفاده از Tasks

به `.vscode/tasks.json` اضافه کنید:

```json
{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Start MCP Inspector",
      "type": "shell",
      "command": "npx @modelcontextprotocol/inspector node ${workspaceFolder}/build/index.js",
      "isBackground": true,
      "problemMatcher": {
        "pattern": {
          "regexp": "^$"
        },
        "background": {
          "activeOnStart": true,
          "beginsPattern": "Inspector",
          "endsPattern": "listening"
        }
      }
    }
  ]
}
```

---

## سناریوهای رایج اشکال‌زدایی

### سناریو ۱: سرور اتصال برقرار نمی‌کند

**نشانه‌ها:** Inspector نمایش می‌دهد "Disconnected" یا در حالت "Connecting..." گیر می‌کند

**چک‌لیست:**
1. ✅ آیا فرمان سرور درست است؟
2. ✅ آیا تمام وابستگی‌ها نصب شده‌اند؟
3. ✅ آیا مسیر سرور مطلق است یا نسبی به دایرکتوری جاری؟
4. ✅ آیا متغیرهای محیطی لازم تنظیم شده‌اند؟

**گام‌های اشکال‌زدایی:**
```bash
# سرور را ابتدا به صورت دستی آزمایش کنید
python -c "import your_server_module; print('OK')"

# بررسی کنید که خطاهای واردسازی وجود نداشته باشد
python -m your_server_module 2>&1 | head -20

# اطمینان حاصل کنید که MCP SDK نصب شده است
pip show mcp
```

### سناریو ۲: ابزارها نمایش داده نمی‌شوند

**نشانه‌ها:** تب ابزارها لیست خالی نشان می‌دهد

**دلایل احتمالی:**
1. ابزارها هنگام راه‌اندازی سرور ثبت نشده‌اند
2. سرور پس از راه‌اندازی دچار خرابی شده است
3. هندلر `tools/list` آرایه خالی برمی‌گرداند

**گام‌های اشکال‌زدایی:**
1. گزارش پیام را برای پاسخ `tools/list` بررسی کنید
2. در کد ثبت ابزار خود لاگ‌گذاری اضافه کنید
3. مطمئن شوید دکوراتورهای `@mcp.tool()` وجود دارند (در پایتون)

### سناریو ۳: ابزار خطا برمی‌گرداند

**نشانه‌ها:** فراخوانی ابزار پاسخ خطا برمی‌گرداند

**رویکرد اشکال‌زدایی:**
1. پیام خطا را با دقت بخوانید
2. بررسی کنید که نوع پارامترها با قالب مطابقت داشته باشد
3. بلوک try/catch با پیام‌های خطای دقیق اضافه کنید
4. لاگ‌های سرور را برای استک‌ترس‌ها بررسی کنید

**نمونه بهبود مدیریت خطا:**

```python
@mcp.tool()
async def my_tool(param1: str, param2: int) -> str:
    try:
        # منطق ابزار در اینجا
        result = process(param1, param2)
        return str(result)
    except ValueError as e:
        raise McpError(f"Invalid parameter: {e}")
    except Exception as e:
        raise McpError(f"Tool failed: {type(e).__name__}: {e}")
```

### سناریو ۴: محتوای منبع خالی است

**نشانه‌ها:** منبع بازگردانده می‌شود ولی محتوا خالی یا null است

**چک‌لیست:**
1. ✅ مسیر فایل یا URI درست است
2. ✅ سرور اجازه خواندن منبع را دارد
3. ✅ محتوای منبع به‌درستی بازگردانده می‌شود

---

## قابلیت‌های پیشرفته Inspector

### هدرهای سفارشی (SSE)

```bash
npx @modelcontextprotocol/inspector \
  --sse http://localhost:8080/sse \
  --header "Authorization: Bearer your-token"
```

### لاگ‌گیری مفصل

```bash
DEBUG=mcp* npx @modelcontextprotocol/inspector python server.py
```

### ضبط جلسات

Inspector می‌تواند گزارش پیام‌ها را برای تحلیل بعدی صادر کند:
1. روی **Export Log** در پنل پیام‌ها کلیک کنید
2. فایل JSON را ذخیره کنید
3. با اعضای تیم برای رفع اشکال به اشتراک بگذارید

---

## بهترین شیوه‌ها

1. **زود و مکرر تست کنید** - از Inspector در طول توسعه استفاده کنید، نه فقط وقتی خطا رخ می‌دهد
2. **به سادگی شروع کنید** - ابتدا اتصال پایه را تست کنید قبل از فراخوانی ابزارهای پیچیده
3. **قالب را بررسی کنید** - بسیاری از خطاها به خاطر ناسازگاری نوع پارامترهاست
4. **پیام‌های خطا را بخوانید** - خطاهای MCP معمولاً توصیفی هستند
5. **Inspector را باز نگه دارید** - کمک می‌کند مشکلات در حین توسعه شناسایی شوند

---

## مرحله بعد

شما ماژول ۳: شروع کار را تکمیل کردید! یادگیری خود را ادامه دهید:

- [ماژول ۴: پیاده‌سازی عملی](../../04-PracticalImplementation/README.md)

---

## منابع اضافی

- [مخزن GitHub MCP Inspector](https://github.com/modelcontextprotocol/inspector)
- [مشخصات MCP - پیام‌های پروتکل](https://spec.modelcontextprotocol.io/specification/2025-11-25/)
- [مشخصات JSON-RPC 2.0](https://www.jsonrpc.org/specification)

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**سلب مسئولیت**:
این سند با استفاده از سرویس ترجمه ماشینی [Co-op Translator](https://github.com/Azure/co-op-translator) ترجمه شده است. در حالی که ما تلاش می‌کنیم دقت را حفظ کنیم، لطفاً توجه داشته باشید که ترجمه‌های خودکار ممکن است شامل خطاها یا نادقیقی‌هایی باشند. سند اصلی به زبان بومی خود باید به عنوان منبع معتبر در نظر گرفته شود. برای اطلاعات حیاتی، ترجمه حرفه‌ای توسط انسان توصیه می‌شود. ما در قبال هرگونه سوءتفاهم یا برداشت نادرست ناشی از استفاده از این ترجمه مسئولیتی نداریم.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->