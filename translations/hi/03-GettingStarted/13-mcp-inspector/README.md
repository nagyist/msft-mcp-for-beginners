# MCP निरीक्षक के साथ डिबगिंग

**MCP निरीक्षक** एक आवश्यक डिबगिंग उपकरण है जो आपको अपने MCP सर्वरों का इंटरैक्टिव रूप से परीक्षण और समस्या निवारण करने देता है बिना पूर्ण AI होस्ट एप्लिकेशन की ज़रूरत के। इसे "MCP के लिए पोस्टमैन" के रूप में सोचें - यह अनुरोध भेजने, प्रतिक्रियाएँ देखने, और आपके सर्वर के व्यवहार को समझने के लिए एक दृश्य इंटरफ़ेस प्रदान करता है।

## MCP निरीक्षक क्यों उपयोग करें?

जब आप MCP सर्वर बना रहे हों, तो अक्सर ये चुनौतियाँ सामने आती हैं:

- **"क्या मेरा सर्वर चालू है?"** - निरीक्षक कनेक्शन स्थिति दिखाता है
- **"क्या मेरे उपकरण सही तरीके से पंजीकृत हैं?"** - निरीक्षक सभी उपलब्ध उपकरणों को सूचीबद्ध करता है
- **"प्रतिक्रिया फॉर्मेट क्या है?"** - निरीक्षक पूरी JSON प्रतिक्रियाएँ दिखाता है
- **"यह उपकरण क्यों काम नहीं कर रहा?"** - निरीक्षक विस्तृत त्रुटि संदेश दिखाता है

## आवश्यकताएं

- Node.js 18+ स्थापित
- npm (Node.js के साथ आता है)
- परीक्षण के लिए एक MCP सर्वर (देखें [Module 3.1 - First Server](../01-first-server/README.md))

## स्थापना

### विकल्प 1: npx के साथ चलाएं (त्वरित परीक्षण के लिए अनुशंसित)

```bash
npx @modelcontextprotocol/inspector
```

### विकल्प 2: ग्लोबली स्थापित करें

```bash
npm install -g @modelcontextprotocol/inspector
mcp-inspector
```

### विकल्प 3: अपने प्रोजेक्ट में जोड़ें

```bash
cd your-mcp-server-project
npm install --save-dev @modelcontextprotocol/inspector
```

को `package.json` में जोड़ें:
```json
{
  "scripts": {
    "inspector": "mcp-inspector"
  }
}
```

---

## अपने सर्वर से कनेक्ट करना

### stdio सर्वर (लोकल प्रोसेस)

उन सर्वरों के लिए जो मानक इनपुट/आउटपुट के माध्यम से संचार करते हैं:

```bash
# पाइथन सर्वर
npx @modelcontextprotocol/inspector python -m your_server_module

# नोड.जेएस सर्वर
npx @modelcontextprotocol/inspector node ./build/index.js

# परिवेश चर के साथ
OPENAI_API_KEY=xxx npx @modelcontextprotocol/inspector python server.py
```

### SSE/HTTP सर्वर (नेटवर्क)

HTTP सेवाओं के रूप में चलने वाले सर्वरों के लिए:

1. पहले अपने सर्वर को शुरू करें:
   ```bash
   python server.py  # सर्वर http://localhost:8080 पर चल रहा है
   ```

2. निरीक्षक शुरू करें और कनेक्ट करें:
   ```bash
   npx @modelcontextprotocol/inspector --sse http://localhost:8080/sse
   ```

---

## निरीक्षक इंटरफ़ेस का अवलोकन

जब निरीक्षक शुरू होता है, तो आपको एक वेब इंटरफ़ेस दिखाई देगा (आमतौर पर `http://localhost:5173` पर):

```
┌─────────────────────────────────────────────────────────────┐
│  MCP Inspector                              [Connected ✅]   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │   🔧 Tools  │  │ 📄 Resources│  │ 💬 Prompts  │         │
│  │    (3)      │  │    (2)      │  │    (1)      │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐ │
│  │  📋 Message Log                                       │ │
│  │  ─────────────────────────────────────────────────── │ │
│  │  → initialize                                         │ │
│  │  ← initialized (server info)                          │ │
│  │  → tools/list                                         │ │
│  │  ← tools (3 tools)                                    │ │
│  └───────────────────────────────────────────────────────┘ │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## उपकरणों का परीक्षण

### उपलब्ध उपकरणों की सूची बनाना

1. **Tools** टैब पर क्लिक करें
2. निरीक्षक स्वतः `tools/list` कॉल करता है
3. आप सभी पंजीकृत उपकरण देखेंगे:
   - उपकरण का नाम
   - विवरण
   - इनपुट स्कीमा (पैरामीटर)

### किसी उपकरण को चलाना

1. सूची से एक उपकरण चुनें
2. फॉर्म में आवश्यक पैरामीटर भरें
3. **Run Tool** पर क्लिक करें
4. परिणाम पैनल में प्रतिक्रिया देखें

**उदाहरण: कैलकुलेटर उपकरण का परीक्षण**

```
Tool: add
Parameters:
  a: 25
  b: 17

Response:
{
  "content": [
    {
      "type": "text",
      "text": "42"
    }
  ]
}
```

### उपकरण त्रुटियों का डिबगिंग

जब कोई उपकरण विफल होता है, निरीक्षक दिखाता है:

```
Error Response:
{
  "error": {
    "code": -32602,
    "message": "Invalid params: 'b' is required"
  }
}
```

सामान्य त्रुटि कोड:
| कोड | अर्थ |
|------|---------|
| -32700 | पार्स त्रुटि (अवैध JSON) |
| -32600 | अवैध अनुरोध |
| -32601 | विधि नहीं मिली |
| -32602 | अवैध पैरामीटर |
| -32603 | आंतरिक त्रुटि |

---

## संसाधनों का परीक्षण

### संसाधनों की सूची बनाना

1. **Resources** टैब पर क्लिक करें
2. निरीक्षक `resources/list` कॉल करता है
3. आप देखेंगे:
   - संसाधन URI
   - नाम और विवरण
   - MIME प्रकार

### किसी संसाधन को पढ़ना

1. एक संसाधन चुनें
2. **Read Resource** पर क्लिक करें
3. लौटाई गई सामग्री देखें

**उदाहरण आउटपुट:**

```
Resource: file:///config/settings.json
Content-Type: application/json

{
  "config": {
    "debug": true,
    "maxConnections": 10
  }
}
```

---

## प्रॉम्प्ट का परीक्षण

### प्रॉम्प्ट की सूची बनाना

1. **Prompts** टैब पर क्लिक करें
2. निरीक्षक `prompts/list` कॉल करता है
3. उपलब्ध प्रॉम्प्ट टेम्प्लेट देखें

### प्रॉम्प्ट प्राप्त करना

1. एक प्रॉम्प्ट चुनें
2. आवश्यक तर्क भरें
3. **Get Prompt** पर क्लिक करें
4. रेंडर किए गए प्रॉम्प्ट संदेश देखें

---

## संदेश लॉग विश्लेषण

संदेश लॉग सभी MCP प्रोटोकॉल संदेश दिखाता है:

```
14:32:01 → {"jsonrpc":"2.0","id":1,"method":"initialize",...}
14:32:01 ← {"jsonrpc":"2.0","id":1,"result":{"protocolVersion":"2025-11-25",...}}
14:32:02 → {"jsonrpc":"2.0","id":2,"method":"tools/list"}
14:32:02 ← {"jsonrpc":"2.0","id":2,"result":{"tools":[...]}}
14:32:05 → {"jsonrpc":"2.0","id":3,"method":"tools/call","params":{"name":"add",...}}
14:32:05 ← {"jsonrpc":"2.0","id":3,"result":{"content":[...]}}
```

### क्या देखना चाहिए

- **अनुरोध/प्रतिक्रिया जोड़े**: प्रत्येक `→` के साथ एक मिलती जुलती `←` होनी चाहिए
- **त्रुटि संदेश**: प्रतिक्रियाओं में `"error"` देखें
- **समय निर्धारण**: बड़े अंतर प्रदर्शन समस्याओं का संकेत हो सकते हैं
- **प्रोटोकॉल संस्करण**: सुनिश्चित करें कि सर्वर और क्लाइंट संस्करण पर सहमत हों

---

## वीएस कोड एकीकरण

आप सीधे VS Code से निरीक्षक चला सकते हैं:

### launch.json का उपयोग

`.vscode/launch.json` में जोड़ें:

```json
{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug with MCP Inspector",
      "type": "node",
      "request": "launch",
      "runtimeExecutable": "npx",
      "runtimeArgs": [
        "@modelcontextprotocol/inspector",
        "python",
        "${workspaceFolder}/server.py"
      ],
      "console": "integratedTerminal"
    },
    {
      "name": "Debug SSE Server with Inspector",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:5173",
      "preLaunchTask": "Start MCP Inspector"
    }
  ]
}
```

### टास्क्स का उपयोग

`.vscode/tasks.json` में जोड़ें:

```json
{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Start MCP Inspector",
      "type": "shell",
      "command": "npx @modelcontextprotocol/inspector node ${workspaceFolder}/build/index.js",
      "isBackground": true,
      "problemMatcher": {
        "pattern": {
          "regexp": "^$"
        },
        "background": {
          "activeOnStart": true,
          "beginsPattern": "Inspector",
          "endsPattern": "listening"
        }
      }
    }
  ]
}
```

---

## सामान्य डिबगिंग परिदृश्य

### परिदृश्य 1: सर्वर कनेक्ट नहीं हो रहा

**लक्षण:** निरीक्षक "Disconnected" दिखाता है या "Connecting..." पर अटका रहता है

**चेकलिस्ट:**
1. ✅ क्या सर्वर कमांड सही है?
2. ✅ क्या सभी निर्भरताएं स्थापित हैं?
3. ✅ क्या सर्वर पथ मौजूदा निर्देशिका के सापेक्ष या पूर्ण है?
4. ✅ क्या आवश्यक पर्यावरण वेरिएबल सेट हैं?

**डिबग चरण:**
```bash
# सर्वर को मैन्युअली पहले जांचें
python -c "import your_server_module; print('OK')"

# आयात त्रुटियों के लिए जांचें
python -m your_server_module 2>&1 | head -20

# सत्यापित करें कि MCP SDK स्थापित है
pip show mcp
```

### परिदृश्य 2: उपकरण प्रदर्शित नहीं हो रहे

**लक्षण:** Tools टैब खाली सूची दिखाता है

**संभावित कारण:**
1. उपकरण सर्वर प्रारंभिककरण के दौरान पंजीकृत नहीं हुए
2. सर्वर स्टार्टअप के बाद क्रैश हो गया
3. `tools/list` हैंडलर खाली सरणी लौटाता है

**डिबग चरण:**
1. संदेश लॉग में `tools/list` प्रतिक्रिया देखें
2. अपने उपकरण पंजीकरण कोड में लॉगिंग जोड़ें
3. सुनिश्चित करें कि `@mcp.tool()` डेकोरेटर मौजूद हैं (Python)

### परिदृश्य 3: उपकरण त्रुटि लौटाता है

**लक्षण:** उपकरण कॉल त्रुटि प्रतिक्रिया देता है

**डिबग दृष्टिकोण:**
1. त्रुटि संदेश ध्यान से पढ़ें
2. पैरामीटर प्रकार स्कीमा से मेल खाते हैं या नहीं जांचें
3. विस्तृत त्रुटि संदेशों के साथ try/catch जोड़ें
4. सर्वर लॉग में स्टैक ट्रेस देखें

**सुधारित त्रुटि हैंडलिंग का उदाहरण:**

```python
@mcp.tool()
async def my_tool(param1: str, param2: int) -> str:
    try:
        # उपकरण तर्क यहाँ
        result = process(param1, param2)
        return str(result)
    except ValueError as e:
        raise McpError(f"Invalid parameter: {e}")
    except Exception as e:
        raise McpError(f"Tool failed: {type(e).__name__}: {e}")
```

### परिदृश्य 4: संसाधन सामग्री खाली है

**लक्षण:** संसाधन लौटता है लेकिन सामग्री खाली या null है

**चेकलिस्ट:**
1. ✅ फ़ाइल पथ या URI सही है
2. ✅ सर्वर के पास संसाधन पढ़ने की अनुमति है
3. ✅ संसाधन सामग्री सही तरीके से लौट रही है

---

## उन्नत निरीक्षक विशेषताएं

### कस्टम हेडर (SSE)

```bash
npx @modelcontextprotocol/inspector \
  --sse http://localhost:8080/sse \
  --header "Authorization: Bearer your-token"
```

### विस्तृत लॉगिंग

```bash
DEBUG=mcp* npx @modelcontextprotocol/inspector python server.py
```

### सत्र रिकॉर्डिंग

निरीक्षक बाद के विश्लेषण के लिए संदेश लॉग एक्सपोर्ट कर सकता है:
1. संदेश पैनल में **Export Log** पर क्लिक करें
2. JSON फ़ाइल सहेजें
3. टीम सदस्यों के साथ साझा करें डिबगिंग के लिए

---

## सर्वोत्तम प्रथाएं

1. **जल्दी और बार-बार परीक्षण करें** - विकास के दौरान निरीक्षक का उपयोग करें, केवल जब चीज़ें टूटें तब नहीं
2. **सरल से शुरू करें** - जटिल उपकरण कॉल से पहले बुनियादी कनेक्टिविटी का परीक्षण करें
3. **स्कीमा जांचें** - कई त्रुटियाँ पैरामीटर प्रकार असंगतियों से आती हैं
4. **त्रुटि संदेश पढ़ें** - MCP त्रुटियाँ आमतौर पर वर्णनात्मक होती हैं
5. **निरीक्षक खुला रखें** - यह विकास के दौरान मुद्दों को पकड़ने में मदद करता है

---

## अगला क्या है

आपने Module 3: Getting Started पूरा कर लिया है! अपनी सीख जारी रखें:

- [Module 4: Practical Implementation](../../04-PracticalImplementation/README.md)

---

## अतिरिक्त संसाधन

- [MCP निरीक्षक GitHub रिपॉजिटरी](https://github.com/modelcontextprotocol/inspector)
- [MCP विनिर्देश - प्रोटोकॉल संदेश](https://spec.modelcontextprotocol.io/specification/2025-11-25/)
- [JSON-RPC 2.0 विनिर्देश](https://www.jsonrpc.org/specification)

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**अस्वीकरण**:  
यह दस्तावेज़ एआई अनुवाद सेवा [Co-op Translator](https://github.com/Azure/co-op-translator) का उपयोग करके अनूदित किया गया है। जबकि हम सटीकता के लिए प्रयासरत हैं, कृपया ध्यान दें कि स्वचालित अनुवादों में त्रुटियाँ या अशुद्धियाँ हो सकती हैं। मूल दस्तावेज़ अपनी स्थानीय भाषा में प्रामाणिक स्रोत माना जाना चाहिए। महत्वपूर्ण जानकारी के लिए, पेशेवर मानव अनुवाद की सलाह दी जाती है। इस अनुवाद के उपयोग से उत्पन्न किसी भी गलतफहमी या गलत अर्थ निष्पादन के लिए हम जिम्मेदार नहीं हैं।
<!-- CO-OP TRANSLATOR DISCLAIMER END -->