# Отладка с помощью MCP Inspector

**MCP Inspector** — это важный инструмент для отладки, который позволяет интерактивно тестировать и устранять неполадки на ваших MCP серверах без необходимости использовать полнофункциональное приложение AI хоста. Можно считать его «Postman для MCP» — он предоставляет визуальный интерфейс для отправки запросов, просмотра ответов и понимания поведения вашего сервера.

## Почему стоит использовать MCP Inspector?

При разработке MCP серверов вы часто столкнетесь с этими задачами:

- **«Мой сервер вообще запущен?»** — Inspector отображает статус подключения  
- **«Правильно ли зарегистрированы мои инструменты?»** — Inspector показывает все доступные инструменты  
- **«Каков формат ответа?»** — Inspector демонстрирует полный JSON ответ  
- **«Почему этот инструмент не работает?»** — Inspector показывает подробные сообщения об ошибках  

## Требования

- Установлен Node.js версии 18 и выше  
- npm (устанавливается вместе с Node.js)  
- MCP сервер для тестирования (см. [Модуль 3.1 — Первый сервер](../01-first-server/README.md))  

## Установка

### Вариант 1: Запуск через npx (рекомендуется для быстрого теста)

```bash
npx @modelcontextprotocol/inspector
```

### Вариант 2: Глобальная установка

```bash
npm install -g @modelcontextprotocol/inspector
mcp-inspector
```

### Вариант 3: Добавление в ваш проект

```bash
cd your-mcp-server-project
npm install --save-dev @modelcontextprotocol/inspector
```

Добавьте в `package.json`:
```json
{
  "scripts": {
    "inspector": "mcp-inspector"
  }
}
```

---

## Подключение к вашему серверу

### Серверы stdio (локальный процесс)

Для серверов, обменивающихся данными через стандартный ввод/вывод:

```bash
# Сервер на Python
npx @modelcontextprotocol/inspector python -m your_server_module

# Сервер на Node.js
npx @modelcontextprotocol/inspector node ./build/index.js

# С переменными окружения
OPENAI_API_KEY=xxx npx @modelcontextprotocol/inspector python server.py
```

### Серверы SSE/HTTP (сетевая работа)

Для серверов, работающих как HTTP сервисы:

1. Сначала запустите ваш сервер:
   ```bash
   python server.py  # Сервер запущен на http://localhost:8080
   ```

2. Запустите Inspector и подключитесь:
   ```bash
   npx @modelcontextprotocol/inspector --sse http://localhost:8080/sse
   ```

---

## Обзор интерфейса Inspector

При запуске Inspector вы увидите веб-интерфейс (обычно по адресу `http://localhost:5173`):

```
┌─────────────────────────────────────────────────────────────┐
│  MCP Inspector                              [Connected ✅]   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │   🔧 Tools  │  │ 📄 Resources│  │ 💬 Prompts  │         │
│  │    (3)      │  │    (2)      │  │    (1)      │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐ │
│  │  📋 Message Log                                       │ │
│  │  ─────────────────────────────────────────────────── │ │
│  │  → initialize                                         │ │
│  │  ← initialized (server info)                          │ │
│  │  → tools/list                                         │ │
│  │  ← tools (3 tools)                                    │ │
│  └───────────────────────────────────────────────────────┘ │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## Тестирование инструментов

### Просмотр доступных инструментов

1. Перейдите на вкладку **Tools**  
2. Inspector автоматически вызывает `tools/list`  
3. Вы увидите все зарегистрированные инструменты с:  
   - Названием инструмента  
   - Описанием  
   - Схемой входных параметров  

### Вызов инструмента

1. Выберите инструмент из списка  
2. Заполните необходимые параметры в форме  
3. Нажмите **Run Tool**  
4. Просмотрите ответ в панели результатов  

**Пример: тестирование калькулятора**

```
Tool: add
Parameters:
  a: 25
  b: 17

Response:
{
  "content": [
    {
      "type": "text",
      "text": "42"
    }
  ]
}
```

### Отладка ошибок инструментов

Когда инструмент завершился с ошибкой, Inspector показывает:

```
Error Response:
{
  "error": {
    "code": -32602,
    "message": "Invalid params: 'b' is required"
  }
}
```

Распространённые коды ошибок:  
| Код    | Значение               |  
|--------|------------------------|  
| -32700 | Ошибка парсинга (некорректный JSON) |  
| -32600 | Некорректный запрос    |  
| -32601 | Метод не найден        |  
| -32602 | Неверные параметры     |  
| -32603 | Внутренняя ошибка      |  

---

## Тестирование ресурсов

### Просмотр ресурсов

1. Перейдите на вкладку **Resources**  
2. Inspector вызывает `resources/list`  
3. Вы увидите:  
   - URI ресурсов  
   - Названия и описания  
   - MIME типы  

### Чтение ресурса

1. Выберите ресурс  
2. Нажмите **Read Resource**  
3. Просмотрите возвращённое содержимое  

**Пример вывода:**

```
Resource: file:///config/settings.json
Content-Type: application/json

{
  "config": {
    "debug": true,
    "maxConnections": 10
  }
}
```

---

## Тестирование подсказок

### Просмотр подсказок

1. Перейдите на вкладку **Prompts**  
2. Inspector вызывает `prompts/list`  
3. Просмотрите доступные шаблоны подсказок  

### Получение подсказки

1. Выберите подсказку  
2. Заполните необходимые аргументы  
3. Нажмите **Get Prompt**  
4. Посмотрите отрендеренные сообщения подсказки  

---

## Анализ журнала сообщений

Журнал сообщений отображает все сообщения протокола MCP:

```
14:32:01 → {"jsonrpc":"2.0","id":1,"method":"initialize",...}
14:32:01 ← {"jsonrpc":"2.0","id":1,"result":{"protocolVersion":"2025-11-25",...}}
14:32:02 → {"jsonrpc":"2.0","id":2,"method":"tools/list"}
14:32:02 ← {"jsonrpc":"2.0","id":2,"result":{"tools":[...]}}
14:32:05 → {"jsonrpc":"2.0","id":3,"method":"tools/call","params":{"name":"add",...}}
14:32:05 ← {"jsonrpc":"2.0","id":3,"result":{"content":[...]}}
```

### На что обратить внимание

- **Пары запросов/ответов**: каждому `→` должен соответствовать `←`  
- **Сообщения об ошибках**: ищите `"error"` в ответах  
- **Временные задержки**: большие паузы могут говорить о проблемах с производительностью  
- **Версия протокола**: убедитесь, что сервер и клиент используют одинаковую версию  

---

## Интеграция с VS Code

Inspector можно запускать прямо из VS Code:

### С помощью launch.json

Добавьте в `.vscode/launch.json`:

```json
{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug with MCP Inspector",
      "type": "node",
      "request": "launch",
      "runtimeExecutable": "npx",
      "runtimeArgs": [
        "@modelcontextprotocol/inspector",
        "python",
        "${workspaceFolder}/server.py"
      ],
      "console": "integratedTerminal"
    },
    {
      "name": "Debug SSE Server with Inspector",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:5173",
      "preLaunchTask": "Start MCP Inspector"
    }
  ]
}
```

### С помощью задач (tasks)

Добавьте в `.vscode/tasks.json`:

```json
{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Start MCP Inspector",
      "type": "shell",
      "command": "npx @modelcontextprotocol/inspector node ${workspaceFolder}/build/index.js",
      "isBackground": true,
      "problemMatcher": {
        "pattern": {
          "regexp": "^$"
        },
        "background": {
          "activeOnStart": true,
          "beginsPattern": "Inspector",
          "endsPattern": "listening"
        }
      }
    }
  ]
}
```

---

## Типичные сценарии отладки

### Сценарий 1: Сервер не подключается

**Симптомы:** Inspector показывает «Disconnected» или зависает на «Connecting...»

**Проверочный список:**  
1. ✅ Правильная команда запуска сервера?  
2. ✅ Установлены все зависимости?  
3. ✅ Путь к серверу абсолютный или относительно текущей директории?  
4. ✅ Установлены необходимые переменные окружения?  

**Шаги отладки:**  
```bash
# Сначала вручную протестируйте сервер
python -c "import your_server_module; print('OK')"

# Проверьте наличие ошибок импорта
python -m your_server_module 2>&1 | head -20

# Убедитесь, что MCP SDK установлен
pip show mcp
```

### Сценарий 2: Инструменты не отображаются

**Симптомы:** Вкладка Tools пуста

**Возможные причины:**  
1. Инструменты не зарегистрированы при инициализации сервера  
2. Сервер упал после запуска  
3. Обработчик `tools/list` возвращает пустой массив  

**Шаги отладки:**  
1. Проверьте журнал сообщений на ответ `tools/list`  
2. Добавьте логирование при регистрации инструментов  
3. Убедитесь, что декораторы `@mcp.tool()` присутствуют (Python)  

### Сценарий 3: Инструмент возвращает ошибку

**Симптомы:** Вызов инструмента возвращает ошибку

**Подход к отладке:**  
1. Внимательно прочитайте сообщение об ошибке  
2. Проверьте соответствие типов параметров схеме  
3. Добавьте try/catch с подробными сообщениями об ошибках  
4. Проверьте логи сервера на наличие трассировок стека  

**Пример улучшенной обработки ошибок:**

```python
@mcp.tool()
async def my_tool(param1: str, param2: int) -> str:
    try:
        # Логика инструмента здесь
        result = process(param1, param2)
        return str(result)
    except ValueError as e:
        raise McpError(f"Invalid parameter: {e}")
    except Exception as e:
        raise McpError(f"Tool failed: {type(e).__name__}: {e}")
```

### Сценарий 4: Содержимое ресурса пустое

**Симптомы:** Ресурс возвращается, но содержимое пустое или null

**Проверочный список:**  
1. ✅ Путь к файлу или URI корректен  
2. ✅ Сервер имеет разрешения на чтение ресурса  
3. ✅ Содержимое ресурса возвращается правильно  

---

## Расширенные возможности Inspector

### Пользовательские заголовки (SSE)

```bash
npx @modelcontextprotocol/inspector \
  --sse http://localhost:8080/sse \
  --header "Authorization: Bearer your-token"
```

### Подробное логирование

```bash
DEBUG=mcp* npx @modelcontextprotocol/inspector python server.py
```

### Запись сессий

Inspector может экспортировать журналы сообщений для последующего анализа:  
1. Нажмите **Export Log** в панели сообщений  
2. Сохраните JSON файл  
3. Поделитесь им с коллегами для отладки  

---

## Лучшие практики

1. **Тестируйте часто и как можно раньше** — используйте Inspector на протяжении всей разработки, а не только при ошибках  
2. **Начинайте с простого** — проверьте базовое соединение перед сложными вызовами инструментов  
3. **Проверяйте схему** — многие ошибки связаны с несовпадением типов параметров  
4. **Читайте сообщения об ошибках** — ошибки MCP обычно описательные  
5. **Держите Inspector открытым** — это помогает своевременно выявлять проблемы во время разработки  

---

## Что дальше

Вы успешно завершили Модуль 3: Начало работы! Продолжайте обучение:

- [Модуль 4: Практическая реализация](../../04-PracticalImplementation/README.md)

---

## Дополнительные ресурсы

- [Репозиторий MCP Inspector на GitHub](https://github.com/modelcontextprotocol/inspector)  
- [Спецификация MCP — сообщения протокола](https://spec.modelcontextprotocol.io/specification/2025-11-25/)  
- [Спецификация JSON-RPC 2.0](https://www.jsonrpc.org/specification)

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**Отказ от ответственности**:  
Этот документ был переведен с помощью сервиса машинного перевода [Co-op Translator](https://github.com/Azure/co-op-translator). Несмотря на наши усилия обеспечить точность, пожалуйста, учитывайте, что автоматический перевод может содержать ошибки или неточности. Оригинальный документ на его исходном языке следует считать авторитетным источником. Для получения важной информации рекомендуется воспользоваться профессиональным переводом, выполненным человеком. Мы не несем ответственности за любые недоразумения или неправильные толкования, возникшие в результате использования данного перевода.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->