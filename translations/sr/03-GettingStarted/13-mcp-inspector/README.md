# Отстрањивање грешака уз MCP Инспектор

**MCP Инспектор** је суштински алат за отстрањивање грешака који вам омогућава интерактивно тестирање и решавање проблема на вашим MCP серверима без потребе за пуном AI хост апликацијом. Замислите га као „Postman за MCP“ – пружа визуелни интерфејс за слање захтева, преглед одговора и разумевање како ваш сервер функционише.

## Зашто користити MCP Инспектор?

При изради MCP сервера често ћете се сусретати са овим изазовима:

- **„Да ли мој сервер уопште ради?“** – Инспектор показује статус везе
- **„Да ли су моји алати исправно регистровани?“** – Инспектор наводи све доступне алате
- **„Који је формат одговора?“** – Инспектор приказује целе JSON одговоре
- **„Зашто овај алат не ради?“** – Инспектор приказује детаљне поруке о грешкама

## Захтеви

- Инсталиран Node.js 18+
- npm (долази уз Node.js)
- MCP сервер за тестирање (погледајте [Модул 3.1 - Први сервер](../01-first-server/README.md))

## Инсталација

### Опција 1: Покретање са npx (Препоручено за брзо тестирање)

```bash
npx @modelcontextprotocol/inspector
```

### Опција 2: Глобална инсталација

```bash
npm install -g @modelcontextprotocol/inspector
mcp-inspector
```

### Опција 3: Додавање у ваш пројекат

```bash
cd your-mcp-server-project
npm install --save-dev @modelcontextprotocol/inspector
```

Додајте у `package.json`:
```json
{
  "scripts": {
    "inspector": "mcp-inspector"
  }
}
```

---

## Повезивање на ваш сервер

### stdio сервери (локални процес)

За сервере који комуницирају преко стандардног улаза/излаза:

```bash
# Питон сервер
npx @modelcontextprotocol/inspector python -m your_server_module

# Ноде.јс сервер
npx @modelcontextprotocol/inspector node ./build/index.js

# Са променљивим окружења
OPENAI_API_KEY=xxx npx @modelcontextprotocol/inspector python server.py
```

### SSE/HTTP сервери (мрежа)

За сервере који раде као HTTP сервиси:

1. Прво покрените ваш сервер:
   ```bash
   python server.py  # Сервер ради на http://localhost:8080
   ```

2. Покрените Инспектор и повежите се:
   ```bash
   npx @modelcontextprotocol/inspector --sse http://localhost:8080/sse
   ```

---

## Преглед интерфејса Инспектора

Када се Инспектор покрене, видећете веб интерфејс (обично на `http://localhost:5173`):

```
┌─────────────────────────────────────────────────────────────┐
│  MCP Inspector                              [Connected ✅]   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │   🔧 Tools  │  │ 📄 Resources│  │ 💬 Prompts  │         │
│  │    (3)      │  │    (2)      │  │    (1)      │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐ │
│  │  📋 Message Log                                       │ │
│  │  ─────────────────────────────────────────────────── │ │
│  │  → initialize                                         │ │
│  │  ← initialized (server info)                          │ │
│  │  → tools/list                                         │ │
│  │  ← tools (3 tools)                                    │ │
│  └───────────────────────────────────────────────────────┘ │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## Тестирање алата

### Листинг доступних алата

1. Кликните на картицу **Tools**
2. Инспектор аутоматски позива `tools/list`
3. Видећете све регистроване алате са:
   - Именом алата
   - Описом
   - Шемом уноса (параметри)

### Позивање алата

1. Изаберите алат са листе
2. Попуните потребне параметре у форми
3. Кликните на **Run Tool**
4. Погледајте одговор у панелу за резултате

**Пример: Тестирање алата калкулатора**

```
Tool: add
Parameters:
  a: 25
  b: 17

Response:
{
  "content": [
    {
      "type": "text",
      "text": "42"
    }
  ]
}
```

### Отстрањивање грешака алата

Када алат не успе, Инспектор приказује:

```
Error Response:
{
  "error": {
    "code": -32602,
    "message": "Invalid params: 'b' is required"
  }
}
```

Чести кодови грешака:
| Код | Значење |
|------|---------|
| -32700 | Грешка при парсирању (неважећи JSON) |
| -32600 | Неважећи захтев |
| -32601 | Метод није пронађен |
| -32602 | Неважећи параметри |
| -32603 | Интерна грешка |

---

## Тестирање ресурса

### Листинг ресурса

1. Кликните на картицу **Resources**
2. Инспектор позива `resources/list`
3. Видећете:
   - URI ресурса
   - Имена и описе
   - MIME типове

### Учитавање ресурса

1. Изаберите ресурс
2. Кликните на **Read Resource**
3. Погледајте враћени садржај

**Пример излаза:**

```
Resource: file:///config/settings.json
Content-Type: application/json

{
  "config": {
    "debug": true,
    "maxConnections": 10
  }
}
```

---

## Тестирање подстицаја (Prompts)

### Листинг подстицаја

1. Кликните на картицу **Prompts**
2. Инспектор позива `prompts/list`
3. Погледајте доступне шаблоне подстицаја

### Добијање подстицаја

1. Изаберите подстицај
2. Попуните потребне аргументе
3. Кликните на **Get Prompt**
4. Погледајте приказане поруке подстицаја

---

## Анализа дневника порука

Дневник порука приказује све MCP протокол поруке:

```
14:32:01 → {"jsonrpc":"2.0","id":1,"method":"initialize",...}
14:32:01 ← {"jsonrpc":"2.0","id":1,"result":{"protocolVersion":"2025-11-25",...}}
14:32:02 → {"jsonrpc":"2.0","id":2,"method":"tools/list"}
14:32:02 ← {"jsonrpc":"2.0","id":2,"result":{"tools":[...]}}
14:32:05 → {"jsonrpc":"2.0","id":3,"method":"tools/call","params":{"name":"add",...}}
14:32:05 ← {"jsonrpc":"2.0","id":3,"result":{"content":[...]}}
```

### Шта треба пратити

- **Парови захтев/одговор**: Сваки `→` треба да има одговарајући `←`
- **Поруке о грешкама**: Потражите `"error"` у одговорима
- **Време**: Велики прекиди могу указивати на проблеме са перформансама
- **Верзија протокола**: Уверите се да сервер и клијент користе сагласну верзију

---

## Интеграција са VS Code

Можете покренути Инспектор директно из VS Code-а:

### Користећи launch.json

Додајте у `.vscode/launch.json`:

```json
{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug with MCP Inspector",
      "type": "node",
      "request": "launch",
      "runtimeExecutable": "npx",
      "runtimeArgs": [
        "@modelcontextprotocol/inspector",
        "python",
        "${workspaceFolder}/server.py"
      ],
      "console": "integratedTerminal"
    },
    {
      "name": "Debug SSE Server with Inspector",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:5173",
      "preLaunchTask": "Start MCP Inspector"
    }
  ]
}
```

### Користећи Tasks

Додајте у `.vscode/tasks.json`:

```json
{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Start MCP Inspector",
      "type": "shell",
      "command": "npx @modelcontextprotocol/inspector node ${workspaceFolder}/build/index.js",
      "isBackground": true,
      "problemMatcher": {
        "pattern": {
          "regexp": "^$"
        },
        "background": {
          "activeOnStart": true,
          "beginsPattern": "Inspector",
          "endsPattern": "listening"
        }
      }
    }
  ]
}
```

---

## Чести сценарији отстрањивања грешака

### Сценарио 1: Сервер се не повезује

**Симптоми:** Инспектор показује „Disconnected“ или заглављује се на „Connecting...“

**Контролна листа:**
1. ✅ Да ли је команда за покретање сервера исправна?
2. ✅ Да ли су све зависности инсталиране?
3. ✅ Да ли је путања до сервера апсолутна или релативна у односу на тренутни директоријум?
4. ✅ Да ли су потребне варијабле окружења подешене?

**Кораци за отклањање грешака:**
```bash
# Прво ручно тестирати сервер
python -c "import your_server_module; print('OK')"

# Проверити за грешке приликом увоза
python -m your_server_module 2>&1 | head -20

# Потврдити да је MCP SDK инсталиран
pip show mcp
```

### Сценарио 2: Алати не приказују уносе

**Симптоми:** Картица алата приказује празну листу

**Могући узроци:**
1. Алати нису регистровани током инициализације сервера
2. Сервер је срушио након покретања
3. `tools/list` обрада враћа празан низ

**Кораци за отклањање грешака:**
1. Проверите дневник порука за одговор `tools/list`
2. Додајте логовање у код регистровања алата
3. Уверите се да постоје `@mcp.tool()` декоратори (Python)

### Сценарио 3: Алат враћа грешку

**Симптоми:** Позив алата враћа поруку о грешци

**Приступ решавању:**
1. Пажљиво прочитајте поруку о грешци
2. Проверите да ли типови параметара одговарају шеми
3. Додајте try/catch блоке са детаљним порукама о грешкама
4. Проверите дневнике сервера за stack trace

**Пример побољшане обраде грешака:**

```python
@mcp.tool()
async def my_tool(param1: str, param2: int) -> str:
    try:
        # Логика алата овде
        result = process(param1, param2)
        return str(result)
    except ValueError as e:
        raise McpError(f"Invalid parameter: {e}")
    except Exception as e:
        raise McpError(f"Tool failed: {type(e).__name__}: {e}")
```

### Сценарио 4: Садржај ресурса је празан

**Симптоми:** Ресурс враћа, али је садржај празан или null

**Контролна листа:**
1. ✅ Путања или URI датотеке су исправни
2. ✅ Сервер има дозволу за читање ресурса
3. ✅ Садржај ресурса се исправно враћа

---

## Напредне функције Инспектора

### Прилагођени хедери (SSE)

```bash
npx @modelcontextprotocol/inspector \
  --sse http://localhost:8080/sse \
  --header "Authorization: Bearer your-token"
```

### Детаљно логовање

```bash
DEBUG=mcp* npx @modelcontextprotocol/inspector python server.py
```

### Снимање сесија

Инспектор може извозити дневнике порука за каснију анализу:
1. Кликните на **Export Log** у панелу за поруке
2. Сачувајте JSON фајл
3. Поделите са сарадницима ради отстрањивања грешака

---

## Најбоље праксе

1. **Тестирајте рано и често** – Користите Инспектор током развоја, не само када нешто пукне
2. **Започните једноставно** – Прво тестирате основну повезивост пре сложених позива алата
3. **Проверите шему** – Многе грешке настају због неслагања типова параметара
4. **Читајте поруке о грешкама** – MCP грешке су обично описне
5. **Држите Инспектор отвореним** – Помаже вам да откријете проблеме док развијате

---

## Шта следи

Завршили сте Модул 3: Почетак рада! Наставите са учењем:

- [Модул 4: Практична примена](../../04-PracticalImplementation/README.md)

---

## Додатни ресурси

- [MCP Inspector GitHub репозиторијум](https://github.com/modelcontextprotocol/inspector)
- [MCP спецификација - поруке протокола](https://spec.modelcontextprotocol.io/specification/2025-11-25/)
- [JSON-RPC 2.0 спецификација](https://www.jsonrpc.org/specification)

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**Одрицање од одговорности**:  
Овај документ је преведен помоћу AI услуге превођења [Co-op Translator](https://github.com/Azure/co-op-translator). Иако тежимо прецизности, молимо вас да имате у виду да аутоматски преводи могу садржати грешке или нетачности. Изворни документ на свом изворном језику треба сматрати ауторитетом. За критичне информације препоручује се професионални људски превод. Нисмо одговорни за било какве неспоразуме или неправилна тумачења која настају као последица коришћења овог превода.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->